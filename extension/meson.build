install_subdir('.',
  install_dir: get_option('datadir') / meson.project_name() / 'extension',
  exclude_files: ['meson.build'],
)
install_data(
  '../data/dbus/systems.misano.LinuxAcademy.Spotlight.xml',
  install_dir: get_option('datadir') / meson.project_name() / 'extension' / 'dbus',
)
